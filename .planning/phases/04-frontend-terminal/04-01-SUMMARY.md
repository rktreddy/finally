---
phase: 04-frontend-terminal
plan: 01
subsystem: ui
tags: [nextjs, typescript, tailwind, sse, eventsource, static-export, dark-theme]

requires:
  - phase: 03-llm-chat-integration
    provides: All backend API endpoints (watchlist, portfolio, trade, chat, SSE streaming)
provides:
  - Next.js 16 project with static export (output: 'export')
  - Dark terminal theme with Tailwind v4 custom colors
  - TypeScript interfaces for all API response shapes
  - Fetch wrappers for all 7 backend endpoints
  - SSE hook with EventSource connection and price history accumulation
  - Header component with portfolio value, cash, connection status
  - CSS grid page layout shell with placeholder panels
  - Pre-generated Watchlist, WatchlistRow, Sparkline, TickerChart, TradeBar components
affects: [04-02-PLAN, 04-03-PLAN, 05-docker-e2e]

tech-stack:
  added: [next@16.2.1, react@19.2.4, typescript@5, tailwindcss@4, lightweight-charts@5.1.0, recharts@3.8.0]
  patterns: [useSSE-hook-for-price-distribution, css-grid-terminal-layout, tailwind-v4-theme-directive, same-origin-fetch-wrappers]

key-files:
  created:
    - frontend/package.json
    - frontend/next.config.ts
    - frontend/app/globals.css
    - frontend/app/layout.tsx
    - frontend/app/page.tsx
    - frontend/lib/types.ts
    - frontend/lib/api.ts
    - frontend/hooks/useSSE.ts
    - frontend/components/Header.tsx
    - frontend/components/Watchlist.tsx
    - frontend/components/WatchlistRow.tsx
    - frontend/components/Sparkline.tsx
    - frontend/components/TickerChart.tsx
    - frontend/components/TradeBar.tsx
  modified:
    - .gitignore

key-decisions:
  - "Used Tailwind v4 @theme inline directive for custom color variables (generated by create-next-app template)"
  - "Root .gitignore updated to negate frontend/lib/ from Python lib/ exclusion"
  - "Kept pre-generated Watchlist, WatchlistRow, Sparkline, TickerChart, TradeBar components from create-next-app template -- they align with Plan 02 requirements"

patterns-established:
  - "useSSE hook: single EventSource connection with price history in useRef, 200-point cap per ticker"
  - "API client: same-origin fetch wrappers with error detail parsing for trade failures"
  - "Dark terminal layout: CSS grid with 1fr + 350px sidebar, panel styling via bg-[#1a1a2e] border-[#30363d]"
  - "Header pattern: live portfolio value computed from SSE prices in page component"

requirements-completed: [UI-01, UI-02, UI-03, UI-04, UI-18]

duration: 6min
completed: 2026-03-22
---

# Phase 4 Plan 01: Frontend Scaffold Summary

**Next.js 16 static export with dark terminal theme, SSE price hook, typed API client, Header component, and grid layout shell**

## Performance

- **Duration:** 6 min
- **Started:** 2026-03-22T15:38:36Z
- **Completed:** 2026-03-22T15:44:50Z
- **Tasks:** 2
- **Files modified:** 16

## Accomplishments
- Next.js 16 project scaffolded with static export, builds successfully to `frontend/out/`
- Dark terminal aesthetic with all project colors configured via Tailwind v4 `@theme inline`
- TypeScript interfaces matching all 7 backend API response shapes
- SSE hook with EventSource lifecycle, connection status tracking, and 200-point price history accumulation
- Header component showing portfolio value, cash balance, and green/yellow/red connection status dot
- CSS grid page layout with placeholder panels for all terminal sections

## Task Commits

Each task was committed atomically:

1. **Task 1: Scaffold Next.js project with types, API client, and SSE hook** - `65fee1d` (feat)
2. **Task 2: Header component and page layout shell** - `a35fbf2` (feat)
3. **Generated assets cleanup** - `646edb6` (chore)

## Files Created/Modified
- `frontend/package.json` - Next.js project with lightweight-charts and recharts dependencies
- `frontend/next.config.ts` - Static export configuration (output: 'export')
- `frontend/app/globals.css` - Tailwind v4 dark theme with all project colors
- `frontend/app/layout.tsx` - Root layout with FinAlly metadata and dark body
- `frontend/app/page.tsx` - Main page with SSE hook, data fetching, grid layout shell
- `frontend/lib/types.ts` - TypeScript interfaces for PriceUpdate, Portfolio, WatchlistItem, ChatResponse, etc.
- `frontend/lib/api.ts` - Fetch wrappers for all 7 API endpoints
- `frontend/hooks/useSSE.ts` - EventSource connection hook with price history accumulation
- `frontend/components/Header.tsx` - Header with portfolio value, cash, connection status dot
- `frontend/components/Watchlist.tsx` - Watchlist panel (pre-generated, Plan 02)
- `frontend/components/WatchlistRow.tsx` - Watchlist row with price flash (pre-generated, Plan 02)
- `frontend/components/Sparkline.tsx` - SVG sparkline (pre-generated, Plan 02)
- `frontend/components/TickerChart.tsx` - Lightweight Charts wrapper (pre-generated, Plan 02)
- `frontend/components/TradeBar.tsx` - Trade execution form (pre-generated, Plan 02)
- `.gitignore` - Added `!frontend/lib/` negation for Python lib/ exclusion

## Decisions Made
- Used Tailwind v4 `@theme inline` directive (generated by create-next-app template) rather than JS config
- Updated root `.gitignore` to negate `frontend/lib/` since Python's `lib/` exclusion pattern blocked frontend TypeScript files
- Kept pre-generated components (Watchlist, WatchlistRow, Sparkline, TickerChart, TradeBar) from create-next-app template since they align well with Plan 02 scope
- Fixed Lightweight Charts UTCTimestamp type cast in TickerChart.tsx (auto-fix, Rule 1)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Root .gitignore blocked frontend/lib/ directory**
- **Found during:** Task 1 (git add)
- **Issue:** Root `.gitignore` had `lib/` for Python packaging, which blocked `frontend/lib/types.ts` and `frontend/lib/api.ts`
- **Fix:** Added `!frontend/lib/` negation rule after `lib/`
- **Files modified:** `.gitignore`
- **Verification:** `git add frontend/lib/types.ts` succeeds
- **Committed in:** `65fee1d` (Task 1 commit)

**2. [Rule 1 - Bug] Fixed Lightweight Charts type error in TickerChart.tsx**
- **Found during:** Task 1 (build verification)
- **Issue:** Pre-generated TickerChart.tsx used `as unknown as number` for time values, but TypeScript rejected it; needed `UTCTimestamp` type from lightweight-charts
- **Fix:** Changed casts to `as UTCTimestamp` and added UTCTimestamp import
- **Files modified:** `frontend/components/TickerChart.tsx`
- **Verification:** `npm run build` succeeds without type errors
- **Committed in:** Part of existing tracked files

---

**Total deviations:** 2 auto-fixed (1 blocking, 1 bug)
**Impact on plan:** Both auto-fixes necessary for build success. No scope creep.

## Issues Encountered
- Parallel agent (04-02) already committed some scaffold files before this agent ran, so Task 1 commit only captured the new lib/ files and .gitignore change. All required files are committed and verified.

## User Setup Required
None - no external service configuration required.

## Known Stubs
- `frontend/app/page.tsx` - Watchlist panel shows "{count} tickers loaded" placeholder text instead of actual Watchlist component (intentional -- Plan 02 will wire real components)
- `frontend/app/page.tsx` - Chart area shows "Select a ticker to view chart" placeholder (intentional -- Plan 02 wires TickerChart)
- `frontend/app/page.tsx` - Trade bar, Positions, Portfolio, P&L, Chat panels all show placeholder text (intentional -- Plans 02 and 03 replace with real components)

All stubs are intentional placeholders that will be replaced by Plans 02 and 03 as designed.

## Next Phase Readiness
- Frontend foundation complete: types, API client, SSE hook, Header, and layout shell all working
- Plan 02 can wire Watchlist, WatchlistRow, Sparkline, TickerChart, and TradeBar into the page (components already exist)
- Plan 03 can add PortfolioHeatmap, PnLChart, PositionsTable, and ChatPanel

## Self-Check: PASSED

All 9 key files verified present. All 3 commit hashes verified in git log.

---
*Phase: 04-frontend-terminal*
*Completed: 2026-03-22*
