---
phase: 04-frontend-terminal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/Sparkline.tsx
  - frontend/components/WatchlistRow.tsx
  - frontend/components/Watchlist.tsx
  - frontend/components/TickerChart.tsx
  - frontend/components/TradeBar.tsx
autonomous: true
requirements:
  - UI-05
  - UI-06
  - UI-07
  - UI-08
  - UI-09
  - UI-10
  - UI-14

must_haves:
  truths:
    - "Watchlist panel shows all tickers with current price, change %, and sparkline"
    - "Prices flash green on uptick and red on downtick with ~500ms fade"
    - "Sparklines accumulate data points progressively from SSE updates"
    - "Clicking a ticker in the watchlist selects it for the main chart"
    - "User can add and remove tickers via UI controls"
    - "Main chart shows price-over-time for the selected ticker using Lightweight Charts"
    - "Trade bar allows entering ticker, quantity, and clicking buy or sell"
  artifacts:
    - path: "frontend/components/Sparkline.tsx"
      provides: "SVG sparkline mini-chart"
      contains: "polyline"
    - path: "frontend/components/WatchlistRow.tsx"
      provides: "Single ticker row with price flash animation"
      contains: "transition-colors"
    - path: "frontend/components/Watchlist.tsx"
      provides: "Full watchlist panel with add/remove controls"
      contains: "addTicker"
    - path: "frontend/components/TickerChart.tsx"
      provides: "Lightweight Charts wrapper for main chart"
      contains: "createChart"
    - path: "frontend/components/TradeBar.tsx"
      provides: "Trade execution form"
      contains: "executeTrade"
  key_links:
    - from: "frontend/components/Watchlist.tsx"
      to: "/api/watchlist"
      via: "addTicker/removeTicker API calls"
      pattern: "addTicker|removeTicker"
    - from: "frontend/components/TickerChart.tsx"
      to: "lightweight-charts"
      via: "createChart import"
      pattern: "import.*createChart"
    - from: "frontend/components/TradeBar.tsx"
      to: "/api/portfolio/trade"
      via: "executeTrade API call"
      pattern: "executeTrade"
---

<objective>
Build the watchlist panel (with sparklines and price flash), main ticker chart, and trade bar components.

Purpose: Delivers the core interactive trading UI -- users can watch live prices, view charts, and execute trades. These are the most visually impactful and functionally critical components.
Output: Five React components ready to be wired into the page layout (wiring happens when page.tsx integrates them alongside Plan 03 components).
</objective>

<execution_context>
@/Users/rkreddy/Ramakrishna/projects/ed_conner_courses/ai_coder/finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rkreddy/Ramakrishna/projects/ed_conner_courses/ai_coder/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-terminal/04-CONTEXT.md
@.planning/phases/04-frontend-terminal/04-RESEARCH.md

<interfaces>
<!-- Types from Plan 01 (lib/types.ts) that these components consume -->

```typescript
// frontend/lib/types.ts
export interface PriceUpdate {
  ticker: string;
  price: number;
  previous_price: number;
  timestamp: number;
  change: number;
  change_percent: number;
  direction: "up" | "down" | "flat";
}
export type PriceMap = Record<string, PriceUpdate>;
export type ConnectionStatus = "connected" | "reconnecting" | "disconnected";

export interface WatchlistItem {
  ticker: string;
  added_at: string;
  price: number | null;
  change: number | null;
  change_percent: number | null;
  direction: string | null;
}

export interface TradeResult {
  trade: { id: string; ticker: string; side: "buy" | "sell"; quantity: number; price: number; executed_at: string; };
  cash_balance: number;
}
```

```typescript
// frontend/hooks/useSSE.ts
export function useSSE(): {
  prices: PriceMap;
  status: ConnectionStatus;
  getHistory: (ticker: string) => { time: number; value: number }[];
}
```

```typescript
// frontend/lib/api.ts
export async function fetchWatchlist(): Promise<WatchlistItem[]>;
export async function addTicker(ticker: string): Promise<any>;
export async function removeTicker(ticker: string): Promise<any>;
export async function executeTrade(ticker: string, quantity: number, side: "buy" | "sell"): Promise<TradeResult>;
```
</interfaces>

NOTE: Plan 01 creates the types, API client, and SSE hook. Plan 02 runs in the same wave so these files may not exist yet at plan start. The executor should verify Plan 01 files exist before building components. If running truly in parallel, these components can be built with the interfaces above as the contract -- they will compile once Plan 01 files are in place.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sparkline, WatchlistRow, and Watchlist components</name>
  <files>
    frontend/components/Sparkline.tsx
    frontend/components/WatchlistRow.tsx
    frontend/components/Watchlist.tsx
  </files>
  <read_first>
    frontend/lib/types.ts
    frontend/lib/api.ts
    frontend/hooks/useSSE.ts
    frontend/app/globals.css
  </read_first>
  <action>
1. Create `frontend/components/Sparkline.tsx` (per D-11, UI-07):
   - No "use client" needed (pure render, no hooks)
   - Props: `{ data: number[], width?: number, height?: number, color?: string }`
   - Default width=80, height=24, color="#209dd7" (accent blue)
   - If data.length < 2, render empty SVG placeholder
   - Calculate min/max of data, normalize to SVG coordinates
   - Render `<svg>` with a `<polyline>` element:
     - `fill="none"` `stroke={color}` `strokeWidth={1.5}`
     - Points calculated as: x = (i / (data.length - 1)) * width, y = height - ((value - min) / range) * (height - 4) - 2
     - Join points as "x1,y1 x2,y2 ..." string for the `points` attribute
   - Use `viewBox={`0 0 ${width} ${height}`}` on the SVG element

2. Create `frontend/components/WatchlistRow.tsx` (per D-09, D-10, D-12, D-13, UI-05, UI-06, UI-08, UI-09):
   - `"use client"` directive
   - Props: `{ ticker: string, price: number | null, change: number | null, changePercent: number | null, direction: string | null, sparklineData: number[], isSelected: boolean, onSelect: (ticker: string) => void, onRemove: (ticker: string) => void }`
   - Price flash animation:
     - Use `useState<"up" | "down" | null>(null)` for flash state
     - Use `useRef(price)` to track previous price
     - In `useEffect` watching `[price, direction]`: if price changed from prevRef, set flash to "up" or "down" based on direction, update prevRef, setTimeout to clear flash after 500ms. Return cleanup to clear timeout.
     - Apply CSS classes: `transition-colors duration-500`
     - When flash === "up": `bg-[rgba(34,197,94,0.25)]`
     - When flash === "down": `bg-[rgba(239,68,68,0.25)]`
     - Otherwise: `bg-transparent`
   - Row layout (table row or flex container):
     - Ticker symbol: `font-bold text-[#e6edf3]` -- clickable, calls `onSelect(ticker)` on click
     - Price: formatted to 2 decimal places, `text-[#e6edf3]`
     - Change %: formatted with sign (+/-) and %, colored green if positive, red if negative, gray if zero
     - Sparkline component with sparklineData
     - Remove button: small "x" icon/text, `text-[#8b949e] hover:text-red-400`, calls `onRemove(ticker)` on click
   - Highlight selected row: if `isSelected`, apply `border-l-2 border-[#209dd7]` or similar left accent

3. Create `frontend/components/Watchlist.tsx` (per D-09, D-13, UI-05, UI-09):
   - `"use client"` directive
   - Props: `{ watchlist: WatchlistItem[], prices: PriceMap, getHistory: (ticker: string) => {time: number, value: number}[], selectedTicker: string | null, onSelectTicker: (ticker: string) => void, onRefresh: () => void }`
   - State: `addInput` string for the add-ticker text input
   - Add ticker section at top:
     - Text input: `bg-[#0d1117] border border-[#30363d] rounded px-2 py-1 text-sm text-[#e6edf3]`
     - "Add" button: `bg-[#209dd7] text-white px-3 py-1 rounded text-sm hover:bg-[#209dd7]/80`
     - On submit: call `addTicker(addInput.toUpperCase())` from api.ts, then call `onRefresh()`, clear input
   - Watchlist rows:
     - For each item in watchlist, render `<WatchlistRow>`:
       - Use `prices[item.ticker]` for live price/direction if available, fall back to item.price/direction from API
       - Pass `getHistory(item.ticker).map(h => h.value)` as sparklineData
       - Pass selectedTicker === item.ticker as isSelected
       - onRemove calls `removeTicker(ticker)` from api.ts then `onRefresh()`
   - Panel styling: `bg-[#1a1a2e] border border-[#30363d] rounded-lg` with heading "Watchlist" in `text-[#8b949e] text-xs uppercase tracking-wider`
   - Scrollable if many tickers: `overflow-y-auto max-h-[400px]`
  </action>
  <verify>
    <automated>cd /Users/rkreddy/Ramakrishna/projects/ed_conner_courses/ai_coder/finally/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/components/Sparkline.tsx` contains `<polyline`, `fill="none"`, `stroke={color}`, `points`
    - `frontend/components/WatchlistRow.tsx` contains `"use client"`, `transition-colors`, `duration-500`, `bg-[rgba(34,197,94,0.25)]`, `bg-[rgba(239,68,68,0.25)]`, `onSelect`, `onRemove`
    - `frontend/components/Watchlist.tsx` contains `"use client"`, `addTicker`, `removeTicker`, `WatchlistRow`, `onRefresh`, `WatchlistItem`
    - TypeScript compilation (`npx tsc --noEmit`) passes without errors
  </acceptance_criteria>
  <done>Watchlist panel renders ticker rows with live prices, price flash animations (green/red 500ms fade), SVG sparklines from SSE history, add/remove ticker controls, and click-to-select behavior.</done>
</task>

<task type="auto">
  <name>Task 2: TickerChart and TradeBar components</name>
  <files>
    frontend/components/TickerChart.tsx
    frontend/components/TradeBar.tsx
  </files>
  <read_first>
    frontend/lib/types.ts
    frontend/lib/api.ts
    frontend/components/Watchlist.tsx
  </read_first>
  <action>
1. Create `frontend/components/TickerChart.tsx` (per D-14, UI-10):
   - `"use client"` directive
   - Props: `{ ticker: string | null, data: { time: number; value: number }[] }`
   - Use dynamic import pattern for Lightweight Charts to avoid SSR issues:
     ```typescript
     import dynamic from "next/dynamic";
     ```
     OR use a guard: only import and create chart inside useEffect (client-side only).
   - Implementation with useRef pattern:
     - `containerRef = useRef<HTMLDivElement>(null)` for the chart container
     - `chartRef = useRef<IChartApi | null>(null)` for the chart instance
     - `seriesRef = useRef<ISeriesApi<"Line"> | null>(null)` for the line series
   - First useEffect (runs once on mount): create chart with `createChart(containerRef.current, {...})`:
     - `layout.background.color: "#0d1117"` (terminal bg)
     - `layout.textColor: "#c9d1d9"`
     - `grid.vertLines.color: "#30363d"`, `grid.horzLines.color: "#30363d"`
     - `width: containerRef.current.clientWidth`, `height: 300`
     - `timeScale: { timeVisible: true, secondsVisible: true }`
     - Add line series: `chart.addSeries(LineSeries, { color: "#209dd7", lineWidth: 2 })`
     - Add resize observer or window resize listener to update chart width
     - Cleanup: remove resize listener, call `chart.remove()`
   - Second useEffect (watches `[data]`): call `seriesRef.current.setData(data.map(d => ({time: d.time as any, value: d.value})))` when data changes
   - When ticker is null: show placeholder text "Select a ticker from the watchlist" in `text-[#8b949e]` centered
   - Panel styling: `bg-[#1a1a2e] border border-[#30363d] rounded-lg p-3`
   - Show ticker name as heading: `text-[#ecad0a] font-bold text-lg`

2. Create `frontend/components/TradeBar.tsx` (per D-18, D-19, UI-14):
   - `"use client"` directive
   - Props: `{ selectedTicker: string | null, onTradeComplete: () => void }`
   - State: `ticker` (string, initialized from selectedTicker), `quantity` (string for input), `feedback` ({ message: string, type: "success" | "error" } | null), `loading` (boolean)
   - useEffect to sync ticker state when selectedTicker prop changes
   - Layout: horizontal flex bar with:
     - Ticker input: `bg-[#0d1117] border border-[#30363d] text-[#e6edf3] rounded px-3 py-2 w-24 uppercase` placeholder "TICKER"
     - Quantity input: `bg-[#0d1117] border border-[#30363d] text-[#e6edf3] rounded px-3 py-2 w-24` placeholder "QTY" type="number" min="0" step="any"
     - Buy button: `bg-[#209dd7] hover:bg-[#209dd7]/80 text-white font-bold px-6 py-2 rounded` text "BUY"
     - Sell button: `bg-[#753991] hover:bg-[#753991]/80 text-white font-bold px-6 py-2 rounded` text "SELL"
   - On buy/sell click:
     - Validate ticker non-empty and quantity > 0
     - Set loading=true
     - Call `executeTrade(ticker.toUpperCase(), parseFloat(quantity), side)` from api.ts
     - On success: set feedback to success message ("Bought 10 AAPL at $190.50"), call `onTradeComplete()`, clear quantity
     - On error: set feedback to error message (from caught error)
     - Set loading=false
     - Clear feedback after 3 seconds via setTimeout
   - Inline feedback area: show feedback.message with green text (success) or red text (error)
   - Disable buttons while loading
   - Panel styling: `bg-[#1a1a2e] border border-[#30363d] rounded-lg p-3`
  </action>
  <verify>
    <automated>cd /Users/rkreddy/Ramakrishna/projects/ed_conner_courses/ai_coder/finally/frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/components/TickerChart.tsx` contains `"use client"`, `createChart`, `LineSeries`, `#0d1117`, `#209dd7`, `containerRef`, `chartRef`, `seriesRef`
    - `frontend/components/TradeBar.tsx` contains `"use client"`, `executeTrade`, `BUY`, `SELL`, `bg-[#209dd7]`, `bg-[#753991]`, `onTradeComplete`
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>Main chart displays price-over-time for selected ticker using Lightweight Charts with dark theme. Trade bar accepts ticker/quantity input and submits buy/sell orders with inline success/error feedback.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes in frontend/ (all types correct)
2. `npm run build` succeeds (static export works)
3. All 5 component files exist with correct imports and "use client" directives
4. Sparkline renders SVG polyline, WatchlistRow has flash animation classes, Watchlist has add/remove controls
5. TickerChart uses Lightweight Charts createChart, TradeBar calls executeTrade
</verification>

<success_criteria>
- Sparkline renders SVG polyline from price data array
- WatchlistRow applies green/red flash CSS transition on price change
- Watchlist shows all tickers with add/remove controls calling the API
- Clicking a ticker in watchlist triggers onSelectTicker callback
- TickerChart creates a Lightweight Charts instance with dark theme styling
- TradeBar submits trades via API and shows inline feedback
- All components compile and build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-terminal/04-02-SUMMARY.md`
</output>
