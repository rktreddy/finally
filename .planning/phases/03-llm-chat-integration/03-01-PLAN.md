---
phase: 03-llm-chat-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/uv.lock
  - backend/app/llm/__init__.py
  - backend/app/llm/models.py
  - backend/app/llm/client.py
  - backend/app/llm/prompts.py
  - backend/app/llm/mock.py
  - backend/app/db/repository.py
  - backend/tests/test_llm.py
autonomous: true
requirements: [LLM-06, LLM-07, LLM-08]

must_haves:
  truths:
    - "LiteLLM is a declared dependency in pyproject.toml and uv.lock"
    - "call_llm() calls LiteLLM acompletion with the correct model, provider, and response_format"
    - "generate_mock_response() returns deterministic ChatResponse objects varying by user message content"
    - "build_system_prompt() assembles a system message with cash, positions, watchlist, and total value"
    - "insert_chat_message() persists a row to chat_messages and returns it"
    - "get_chat_history() returns the last N messages in chronological order"
  artifacts:
    - path: "backend/app/llm/models.py"
      provides: "Pydantic models for structured LLM output"
      exports: ["ChatResponse", "TradeAction", "WatchlistAction"]
    - path: "backend/app/llm/client.py"
      provides: "Async LLM call via LiteLLM"
      exports: ["call_llm"]
    - path: "backend/app/llm/prompts.py"
      provides: "System prompt builder with portfolio context"
      exports: ["build_system_prompt", "build_messages"]
    - path: "backend/app/llm/mock.py"
      provides: "Deterministic mock response generator"
      exports: ["generate_mock_response"]
    - path: "backend/app/llm/__init__.py"
      provides: "Public API re-exports"
    - path: "backend/app/db/repository.py"
      provides: "Chat message persistence functions"
      contains: "insert_chat_message"
    - path: "backend/tests/test_llm.py"
      provides: "Unit tests for LLM module"
  key_links:
    - from: "backend/app/llm/client.py"
      to: "litellm.acompletion"
      via: "async call with response_format=ChatResponse"
      pattern: "acompletion.*response_format"
    - from: "backend/app/llm/mock.py"
      to: "backend/app/llm/models.py"
      via: "returns ChatResponse instances"
      pattern: "ChatResponse\\("
---

<objective>
Create the LLM module (backend/app/llm/) with Pydantic models, async LiteLLM client, system prompt builder, mock response generator, and chat repository functions.

Purpose: Establishes all LLM building blocks that the chat route (Plan 02) wires together. Each component is independently testable.
Output: Complete backend/app/llm/ package, chat repository functions in repository.py, unit tests, and litellm declared as a dependency.
</objective>

<execution_context>
@/Users/rkreddy/Ramakrishna/projects/ed_conner_courses/ai_coder/finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rkreddy/Ramakrishna/projects/ed_conner_courses/ai_coder/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-llm-chat-integration/03-CONTEXT.md
@.planning/phases/03-llm-chat-integration/03-RESEARCH.md

<interfaces>
<!-- Existing repository pattern from backend/app/db/repository.py -->
<!-- All functions take aiosqlite.Connection as first arg -->
<!-- Standalone functions commit; transaction functions do NOT commit -->

From backend/app/db/repository.py:
```python
async def add_watchlist_ticker(db: aiosqlite.Connection, ticker: str, user_id: str = "default") -> dict:
async def remove_watchlist_ticker(db: aiosqlite.Connection, ticker: str, user_id: str = "default") -> int:
async def get_user_cash(db: aiosqlite.Connection, user_id: str = "default") -> float:
async def get_positions(db: aiosqlite.Connection, user_id: str = "default") -> list[dict]:
async def get_position(db: aiosqlite.Connection, ticker: str, user_id: str = "default") -> dict | None:
async def update_cash_balance(db: aiosqlite.Connection, new_balance: float, user_id: str = "default") -> None:
async def upsert_position(db: aiosqlite.Connection, ticker: str, quantity: float, avg_cost: float, user_id: str = "default") -> None:
async def delete_position(db: aiosqlite.Connection, ticker: str, user_id: str = "default") -> None:
async def insert_trade(db: aiosqlite.Connection, ticker: str, side: str, quantity: float, price: float, user_id: str = "default") -> dict:
```

From backend/app/market/cache.py:
```python
class PriceCache:
    def get_price(self, ticker: str) -> float | None: ...
    def get(self, ticker: str) -> PriceUpdate | None: ...
    def get_all(self) -> dict[str, PriceUpdate]: ...
```

From backend/app/db/schema.sql (chat_messages table):
```sql
CREATE TABLE IF NOT EXISTS chat_messages (
    id TEXT PRIMARY KEY,
    user_id TEXT NOT NULL DEFAULT 'default',
    role TEXT NOT NULL CHECK(role IN ('user', 'assistant')),
    content TEXT NOT NULL,
    actions TEXT,
    created_at TEXT NOT NULL DEFAULT (datetime('now'))
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add litellm dependency and create LLM module with models, client, prompts, and mock</name>
  <files>
    backend/pyproject.toml,
    backend/uv.lock,
    backend/app/llm/__init__.py,
    backend/app/llm/models.py,
    backend/app/llm/client.py,
    backend/app/llm/prompts.py,
    backend/app/llm/mock.py
  </files>
  <read_first>
    backend/pyproject.toml,
    backend/app/db/repository.py,
    backend/app/routes/portfolio.py,
    backend/app/market/cache.py,
    .claude/skills/cerebras/SKILL.md
  </read_first>
  <action>
1. Add litellm to the project: `cd backend && uv add litellm`

2. Create `backend/app/llm/models.py` (per D-05, D-06, D-07):
   - `from __future__ import annotations`
   - Module docstring: `"""Pydantic models for LLM structured output."""`
   - `from pydantic import BaseModel, Field`
   - `class TradeAction(BaseModel)` with fields: `ticker: str`, `side: str` (buy/sell), `quantity: float`
   - `class WatchlistAction(BaseModel)` with fields: `ticker: str`, `action: str` (add/remove)
   - `class ChatResponse(BaseModel)` with fields: `message: str`, `trades: list[TradeAction] = Field(default_factory=list)`, `watchlist_changes: list[WatchlistAction] = Field(default_factory=list)`

3. Create `backend/app/llm/client.py` (per D-01, D-02, LLM-08):
   - `from __future__ import annotations`
   - Module docstring: `"""LLM client for calling OpenRouter via LiteLLM."""`
   - `import logging` with `logger = logging.getLogger(__name__)`
   - `from litellm import acompletion`
   - `from pydantic import ValidationError`
   - `from .models import ChatResponse`
   - Constants: `MODEL = "openrouter/openai/gpt-oss-120b"`, `EXTRA_BODY = {"provider": {"order": ["cerebras"]}}`
   - `async def call_llm(messages: list[dict]) -> ChatResponse:` that calls `await acompletion(model=MODEL, messages=messages, response_format=ChatResponse, reasoning_effort="low", extra_body=EXTRA_BODY, timeout=30, num_retries=1)`, extracts `response.choices[0].message.content`, and returns `ChatResponse.model_validate_json(content)`
   - Wrap the `model_validate_json` in try/except `ValidationError` — on failure, log the error and return `ChatResponse(message=content)` with empty trades/watchlist_changes (per D-06)
   - Also wrap the entire acompletion call in try/except `Exception` — on failure, log the error and return `ChatResponse(message="I'm sorry, I encountered an error processing your request. Please try again.")`

4. Create `backend/app/llm/prompts.py` (per D-08, D-09, D-10, D-11):
   - `from __future__ import annotations`
   - Module docstring: `"""System prompt builder and context assembly for LLM calls."""`
   - `def build_system_prompt(cash: float, positions: list[dict], watchlist: list[dict], total_value: float) -> str:` that returns a system prompt string containing:
     - Role: "You are FinAlly, an AI trading assistant."
     - Instructions: analyze portfolio, suggest trades with reasoning, execute trades when asked, manage watchlist, be concise and data-driven, always respond with valid JSON matching the response schema
     - Portfolio context formatted as structured text: cash balance, total portfolio value, positions table (ticker, qty, avg_cost, current_price, unrealized_pnl), watchlist tickers with prices
     - Response schema description: message (required), trades array (optional), watchlist_changes array (optional)
   - `def build_messages(system_prompt: str, history: list[dict], user_message: str) -> list[dict]:` that returns `[{"role": "system", "content": system_prompt}] + [{"role": msg["role"], "content": msg["content"]} for msg in history] + [{"role": "user", "content": user_message}]`

5. Create `backend/app/llm/mock.py` (per D-19, D-20, D-21):
   - `from __future__ import annotations`
   - Module docstring: `"""Deterministic mock response generator for LLM_MOCK=true."""`
   - `from .models import ChatResponse, TradeAction, WatchlistAction`
   - `def generate_mock_response(user_message: str) -> ChatResponse:` that checks `user_message.lower()`:
     - Contains "buy" -> `ChatResponse(message="I've placed a buy order for 10 shares of AAPL.", trades=[TradeAction(ticker="AAPL", side="buy", quantity=10)])`
     - Contains "sell" -> `ChatResponse(message="I've placed a sell order for 5 shares of AAPL.", trades=[TradeAction(ticker="AAPL", side="sell", quantity=5)])`
     - Contains "add" -> `ChatResponse(message="I've added PYPL to your watchlist.", watchlist_changes=[WatchlistAction(ticker="PYPL", action="add")])`
     - Contains "remove" -> `ChatResponse(message="I've removed PYPL from your watchlist.", watchlist_changes=[WatchlistAction(ticker="PYPL", action="remove")])`
     - Default -> `ChatResponse(message="Your portfolio is well-diversified across tech and finance sectors. Your total value reflects current market conditions.")`

6. Create `backend/app/llm/__init__.py`:
   - Module docstring: `"""LLM integration for FinAlly chat assistant."""`
   - `from .client import call_llm`
   - `from .mock import generate_mock_response`
   - `from .models import ChatResponse, TradeAction, WatchlistAction`
   - `from .prompts import build_messages, build_system_prompt`
   - `__all__ = ["ChatResponse", "TradeAction", "WatchlistAction", "call_llm", "generate_mock_response", "build_system_prompt", "build_messages"]`
  </action>
  <verify>
    <automated>cd /Users/rkreddy/Ramakrishna/projects/ed_conner_courses/ai_coder/finally/backend && uv run python -c "from app.llm import ChatResponse, TradeAction, WatchlistAction, call_llm, generate_mock_response, build_system_prompt, build_messages; print('All imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/pyproject.toml contains `"litellm`
    - backend/app/llm/models.py contains `class ChatResponse(BaseModel):`
    - backend/app/llm/models.py contains `class TradeAction(BaseModel):`
    - backend/app/llm/models.py contains `class WatchlistAction(BaseModel):`
    - backend/app/llm/client.py contains `async def call_llm(messages: list[dict]) -> ChatResponse:`
    - backend/app/llm/client.py contains `acompletion(`
    - backend/app/llm/client.py contains `response_format=ChatResponse`
    - backend/app/llm/client.py contains `MODEL = "openrouter/openai/gpt-oss-120b"`
    - backend/app/llm/client.py contains `EXTRA_BODY = {"provider": {"order": ["cerebras"]}}`
    - backend/app/llm/prompts.py contains `def build_system_prompt(`
    - backend/app/llm/prompts.py contains `def build_messages(`
    - backend/app/llm/mock.py contains `def generate_mock_response(user_message: str) -> ChatResponse:`
    - backend/app/llm/__init__.py contains `__all__`
    - Import command exits 0
  </acceptance_criteria>
  <done>All LLM module files created with correct exports. litellm added to pyproject.toml and uv.lock. Imports succeed.</done>
</task>

<task type="auto">
  <name>Task 2: Add chat repository functions and unit tests for LLM module</name>
  <files>
    backend/app/db/repository.py,
    backend/tests/test_llm.py
  </files>
  <read_first>
    backend/app/db/repository.py,
    backend/app/llm/models.py,
    backend/app/llm/client.py,
    backend/app/llm/prompts.py,
    backend/app/llm/mock.py,
    backend/app/db/schema.sql,
    backend/tests/test_portfolio.py
  </read_first>
  <action>
1. Add chat repository functions to `backend/app/db/repository.py` (per D-25):
   - Add `"insert_chat_message"` and `"get_chat_history"` to the `__all__` list
   - Add a `# --- Chat ---` section after the Portfolio Snapshots section
   - `async def insert_chat_message(db: aiosqlite.Connection, role: str, content: str, actions: str | None = None, user_id: str = "default") -> dict:`
     - Generate `msg_id = str(uuid.uuid4())`
     - `await db.execute("INSERT INTO chat_messages (id, user_id, role, content, actions) VALUES (?, ?, ?, ?, ?)", (msg_id, user_id, role, content, actions))`
     - `await db.commit()`
     - Read back and return: `async with db.execute("SELECT id, role, content, actions, created_at FROM chat_messages WHERE id = ?", (msg_id,)) as cursor:` -> `return dict(await cursor.fetchone())`
   - `async def get_chat_history(db: aiosqlite.Connection, user_id: str = "default", limit: int = 20) -> list[dict]:`
     - `async with db.execute("SELECT role, content, actions, created_at FROM chat_messages WHERE user_id = ? ORDER BY created_at DESC LIMIT ?", (user_id, limit)) as cursor:`
     - `rows = await cursor.fetchall()`
     - `return [dict(row) for row in reversed(rows)]` (chronological order)

2. Create `backend/tests/test_llm.py` with unit tests:
   - `from __future__ import annotations`
   - `import json`, `import os`, `from unittest.mock import AsyncMock, patch`
   - `import pytest`
   - `from app.llm.models import ChatResponse, TradeAction, WatchlistAction`
   - `from app.llm.mock import generate_mock_response`
   - `from app.llm.prompts import build_system_prompt, build_messages`

   Tests for models:
   - `test_chat_response_minimal`: `ChatResponse(message="hello")` succeeds, `trades == []`, `watchlist_changes == []`
   - `test_chat_response_with_trades`: `ChatResponse(message="ok", trades=[TradeAction(ticker="AAPL", side="buy", quantity=10)])` succeeds, len(trades) == 1
   - `test_chat_response_from_json`: `ChatResponse.model_validate_json('{"message": "hi"}')` succeeds, trades/watchlist_changes are empty lists
   - `test_chat_response_full_json`: Parse full JSON with message, trades, watchlist_changes

   Tests for mock (LLM-07):
   - `test_mock_buy`: `generate_mock_response("please buy some shares")` returns ChatResponse with trades containing side="buy"
   - `test_mock_sell`: `generate_mock_response("sell my AAPL")` returns ChatResponse with trades containing side="sell"
   - `test_mock_add_watchlist`: `generate_mock_response("add PYPL")` returns ChatResponse with watchlist_changes containing action="add"
   - `test_mock_remove_watchlist`: `generate_mock_response("remove PYPL")` returns ChatResponse with watchlist_changes containing action="remove"
   - `test_mock_default`: `generate_mock_response("how is my portfolio?")` returns ChatResponse with non-empty message and empty trades/watchlist_changes

   Tests for prompts:
   - `test_build_system_prompt_contains_context`: Call with cash=10000.0, positions=[{"ticker": "AAPL", "quantity": 10, "avg_cost": 150.0, "current_price": 155.0, "unrealized_pnl": 50.0}], watchlist=[{"ticker": "AAPL", "price": 155.0}], total_value=11550.0. Assert result contains "10000", "AAPL", "FinAlly"
   - `test_build_messages_structure`: Call with a system prompt, history=[{"role": "user", "content": "hi"}, {"role": "assistant", "content": "hello"}], user_message="what's up". Assert first message has role="system", last has role="user" and content="what's up", length is 4

   Tests for client (LLM-08):
   - `test_call_llm_success`: Patch `litellm.acompletion` with AsyncMock returning a mock response object with `choices[0].message.content = '{"message": "test response"}'`. Call `await call_llm([{"role": "user", "content": "hi"}])`. Assert result is ChatResponse with message="test response". Assert acompletion was called with `model="openrouter/openai/gpt-oss-120b"`, verify `extra_body` contains `{"provider": {"order": ["cerebras"]}}`.
   - `test_call_llm_invalid_json`: Patch acompletion to return content="not json at all". Call `await call_llm(...)`. Assert result is ChatResponse with message containing the raw text, trades and watchlist_changes are empty.
   - `test_call_llm_api_error`: Patch acompletion to raise `Exception("API down")`. Call `await call_llm(...)`. Assert result is ChatResponse with a user-friendly error message, trades and watchlist_changes are empty.

   Tests for chat repository functions:
   - Use `_db_in_temp` fixture pattern from test_portfolio.py
   - `test_insert_chat_message`: Insert a message with role="user", content="hello". Assert returned dict has "id", "role"=="user", "content"=="hello", "actions" is None.
   - `test_get_chat_history_chronological`: Insert 3 messages in order. Call `get_chat_history(db, limit=20)`. Assert returned in chronological order (first inserted is first in list).
   - `test_get_chat_history_limit`: Insert 5 messages. Call `get_chat_history(db, limit=3)`. Assert returns exactly 3 messages (the most recent 3).
   - `test_insert_chat_message_with_actions`: Insert with actions=json.dumps({"trades": [{"ticker": "AAPL", "side": "buy"}]}). Assert returned dict has actions != None.

   For DB test fixtures, create a fixture that initializes a fresh DB:
   ```python
   @pytest.fixture
   async def db(tmp_path):
       from app.db import init_db
       db_conn = await init_db(str(tmp_path / "test.db"))
       yield db_conn
       await db_conn.close()
   ```
  </action>
  <verify>
    <automated>cd /Users/rkreddy/Ramakrishna/projects/ed_conner_courses/ai_coder/finally/backend && uv run --extra dev pytest tests/test_llm.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/db/repository.py contains `async def insert_chat_message(`
    - backend/app/db/repository.py contains `async def get_chat_history(`
    - backend/app/db/repository.py __all__ list contains `"insert_chat_message"`
    - backend/app/db/repository.py __all__ list contains `"get_chat_history"`
    - backend/tests/test_llm.py contains `test_mock_buy`
    - backend/tests/test_llm.py contains `test_mock_sell`
    - backend/tests/test_llm.py contains `test_call_llm_success`
    - backend/tests/test_llm.py contains `test_call_llm_invalid_json`
    - backend/tests/test_llm.py contains `test_insert_chat_message`
    - backend/tests/test_llm.py contains `test_get_chat_history`
    - All tests in test_llm.py pass (exit 0)
    - Full test suite still passes: `cd backend && uv run --extra dev pytest -x -q` exits 0
  </acceptance_criteria>
  <done>Chat repository functions added to repository.py. Unit tests for models, mock, prompts, client, and repository all pass. Existing test suite unbroken.</done>
</task>

</tasks>

<verification>
- `cd backend && uv run --extra dev pytest tests/test_llm.py -x -v` — all LLM unit tests pass
- `cd backend && uv run --extra dev pytest -x -q` — full suite passes (no regressions)
- `cd backend && uv run python -c "from app.llm import ChatResponse, call_llm, generate_mock_response, build_system_prompt, build_messages"` — all imports work
</verification>

<success_criteria>
- LiteLLM declared in pyproject.toml, uv.lock updated
- backend/app/llm/ package complete with models.py, client.py, prompts.py, mock.py, __init__.py
- Chat repository functions (insert_chat_message, get_chat_history) added to repository.py
- All unit tests pass covering models, client (mocked), prompts, mock responses, and chat persistence
- Full existing test suite passes without regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-llm-chat-integration/03-01-SUMMARY.md`
</output>
