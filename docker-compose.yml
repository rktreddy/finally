services:
  app:
    build: .
    container_name: finally
    image: finally
    ports:
      - "8000:8000"
    volumes:
      - finally-data:/app/db
    env_file:
      - .env

volumes:
  finally-data:
